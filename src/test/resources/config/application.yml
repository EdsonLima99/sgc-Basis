spring:
    h2:
        console:
            enabled: true
            path: /h2
    datasource:
        url: jdbc:h2:mem:sgp
        username: src
        password: src
        driverClassName: org.h2.Driver
    liquibase:
        change-log: classpath:/config/liquibase/master.xml

server:
    port: 8080
